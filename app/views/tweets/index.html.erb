<h1 align="center">Tweets listing</h1>

<div class="row">
  <div class="col-xs-4 col-xs-offset-4">
    <% @tweets.each do |tweet| %>
      <div class="well">
        <%= tweet.content %>
      </div>
    <% end %>
  </div>
</div>
